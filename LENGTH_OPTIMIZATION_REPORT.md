# 层纹边缘与长度测量优化验证报告

## 总体评估：可用于像素长度与相对比较，但绝对物理长度需补充标定

本次优化已完成并通过配置的整体守卫。共检测 100 条可见亮层纹，其中 99 条通过逐层像素长度质量检查；L20 因内部重复性不确定度达到 3.411 px，被明确标记为人工复核。

增强图适合帮助观察端点和中心路径；CSV 中的 `recommended_*` 长度仍由原始 16 位 TIFF 的中心路径和亚像素端点拟合得到，避免用锐化后像素替代原始测量证据。

## 数据及测量粒度

- 数据源：`input/source_16bit.tif`
- 图像尺寸：1600 × 2200
- 数据类型：单通道 uint16
- 测量粒度：每条可见亮层纹一行，共 100 行
- 左侧：49 条；右侧：51 条
- 长度单位：像素
- 数据日期：文件本身没有采集时间元数据；本实验于 2026-09-05 执行

## 数据质量检查

| 检查 | 结果 | 影响 |
|---|---:|---|
| 位深 | 16 bit | 满足保留原始灰度的要求 |
| TIFF 页数 | 1 | 本次只能做单图内部自监督和空间重复性检查 |
| XResolution/YResolution | 缺失 | 不能换算为毫米等绝对单位 |
| ResolutionUnit | 缺失 | 不能从 TIFF 自行推断物理尺度 |
| 全图零值比例 | 53.86% | 主要来自黑色背景；不能用于估计目标区噪声 |
| 左/右测量区零值比例 | 0.61% / 1.13% | 局部可能含截断背景或边缘，应保留质量标志 |
| 全图 65535 饱和比例 | 0.089% | 饱和主要为极亮局部；测量区饱和比例很低 |
| 训练区 65535 饱和比例 | 0.0067% | 不构成整体端点定位的主要风险 |

### 关键数据质量限制

1. **高严重度：缺少像素尺寸。** 当前结果只能报告像素长度。要得到毫米长度，必须提供同一成像设置下经标准件标定的 `mm/pixel`。
2. **高严重度：缺少系统 PSF/MTF。** 亚像素拟合提高的是数字定位重复性，不能证明已突破成像系统物理分辨率。
3. **中严重度：只有单幅图。** 当前不确定度来自相邻中心路径和增强前后稳定性，尚未覆盖重复扫描、装夹和曝光变化。
4. **中严重度：L20 端点不确定度为 3.411 px。** 该层必须人工检查或用重复扫描重新估计。

## 方法调整

### 1. 上下端点原始数据锚定

长度依赖上下端点。代码在 `y=600:790` 和 `y=1010:1240` 两个端点搜索带内恢复原始 16 位数据权重，防止上一阶段沿竖直方向的连续性平滑移动端点。

### 2. 零相位对称锐化

端点增强采用对称高斯反锐化，`sigma=0.90`、强度 `0.24`，总改变量受噪声尺度限幅。对称核本身不引入方向性相位偏移。

### 3. 弯曲中心路径跟踪

固定竖直采样会在弯曲端部偏离层纹。新版从主体中部开始逐行向上、向下跟踪亮层中心，每行移动不超过 2 px，并把路径约束在相邻层间距的 38% 内，避免跳层。

### 4. 亚像素端点

沿中心路径提取强度剖面，经 `sigma=1.05` 的一维平滑后计算梯度。顶部取最大正梯度，底部取最大负梯度，并使用三点二次曲线拟合获得亚像素位置。

### 5. 推荐值与增强诊断分离

- `recommended_*`：原始 16 位数据的路径拟合结果，作为推荐像素测量值；
- `enhanced_*`：增强图重新检测结果，只用于检查锐化是否造成端点漂移；
- `enhancement_diagnostic`：增强结果的独立质量标志；
- `quality`：推荐原始数据测量值的质量标志。

## 计算复核

| 指标 | 结果 |
|---|---:|
| 可见层纹数 | 100 |
| 测量通过数 | 99 |
| 人工复核数 | 1（L20） |
| 通过比例 | 99% |
| 中位长度不确定度 | 0.369 px |
| 95% 长度不确定度 | 0.991 px |
| 增强前后端点偏移绝对中位数 | 0.012 px |
| 增强前后端点偏移绝对 95% 分位 | 0.089 px |
| 增强前后长度差绝对中位数 | 0.016 px |
| 增强前后长度差绝对 95% 分位 | 0.231 px |
| 同坐标端点梯度中位增益 | 1.0219（+2.19%） |
| 梯度增益 5% 分位 | 1.0091（+0.91%） |
| 增强 ROI 相对原图 SSIM | 0.996388 |
| 整体守卫 | 通过 |

增强图重新检测的最大端点偏移为 2.324 px，但仅集中在一个需复核的重检测诊断；推荐长度不使用该重检测值。

## L20 复核项

- 层编号：L20
- 中心 x：约 606.12 px
- 推荐顶部 y：约 685.08 px
- 推荐底部 y：约 1177.28 px
- 推荐长度：约 492.20 px
- 内部长度不确定度：3.411 px

应在叠加图中检查该层的上下端点。若绝对长度重要，建议对 L20 做手工端点确认或使用同位置重复采集。

## 验证结论

### 已验证

- 代码读取和输出均保持完整 1600 × 2200 uint16 图像；
- 推荐长度来自原始数据，而不是生成式或锐化结果；
- 中心路径跟踪显著降低了固定竖线采样造成的不确定度；
- 99% 层纹达到当前像素级重复性标准；
- 端点梯度整体得到小幅、稳定提升；
- 增强区域与原图保持很高的一致性。

### 尚未验证

- 毫米或微米级绝对长度；
- 系统 PSF/MTF 去卷积后的真实边缘位置；
- 重复扫描、不同曝光及重新装夹条件下的测量重复性；
- 与显微测量或标准件长度的外部一致性。

因此本结果的对外表述应为：“已获得经过原始数据锚定的像素级层纹长度候选；99/100 层通过内部质量检查。”不应表述为“已完成绝对长度计量认证”。
