# v13 盲去噪图直接引导与结构细节一致性报告

## 1. 目标

v11 已能先从配准盲去噪图测量100条层纹与98个夹层，再用解析硬投影锁定生成结果的中心线、端点、长度和FWHM。其不足是盲去噪图只参与“测量约束”，没有直接参与“图像细节”；硬投影重绘的层纹容易沿长度方向过度均匀。

v13 返回 v11 的有限宽片层渲染主线，不采用 v12 更陡的边缘常数，并新增一条同坐标结构细节通道：同一张盲去噪图既用于测量几何，也作为生成模型和最终硬投影的直接细节引导。目标是在不改变长宽的前提下，保留真实的纵向灰度起伏、低频衰减、中央实体块结构和周边目标连续性。

## 2. 四类输入的角色隔离

```text
原始16位图 ───────────────────────> 唯一场景、视野和内容目标
     │
     └─盲去噪─> 配准引导图 ─┬─测量─> v11几何约束图/CSV
                            ├──────> 生成模型同坐标细节引导
                            └──────> 硬投影轴向细节引导
外观参考图 ───────────────────────> 只定义洁净边界与灰度风格
```

- 原始图决定完整画面和待恢复内容。
- 盲去噪图与原图坐标一致，提供几何测量和真实结构细节。
- 约束图只编码49条左侧层纹、51条右侧层纹及98个夹层的几何。
- 未配准参考图只提供视觉风格，不能提供坐标、层数或尺寸。

## 3. 方法

### 3.1 生成阶段的直接细节引导

生成模型同时读取原图、盲去噪图、v11约束图和外观参考。提示词明确要求盲去噪图的低/中频衰减、每条层纹的纵向亮度变化和中央块内部结构必须保持在原坐标；约束图优先于生成美学，禁止增删、合并、分裂或移动层纹。

### 3.2 稳健强度匹配与前景结构融合

盲去噪图首先用 `1%–99.5%` 分位窗映射到生成候选的显示范围，不执行空间变换。覆盖两侧层纹束和中央实体块的羽化前景区域内，结构底图为：

\[
B=(1-\lambda)G_{\sigma}(I_{gen})+\lambda G_{\sigma}(I_{guide}),
\qquad \lambda=0.55
\]

层纹束内部采用足以抑制生成错误周期的横向低通；中央块及非片层结构保留轻度平滑后的引导细节。

### 3.3 每条层纹的轴向结构曲线

沿第 `i` 条固定中心线，在每行采样层纹中心与左右夹层的对比度：

\[
c_i(y)=I(y,p_i(y))-\frac{I(y,p_i(y)-d_i)+I(y,p_i(y)+d_i)}{2}
\]

`c_i(y)` 经稳健归一化和纵向平滑后裁剪至 `[0.58, 1.42]`，只调制解析片层亮度，不参与中心线、宽度或端点计算。距端点 `4–18 px` 的区域把调制逐渐退回1，避免真实亮度变化影响半高端点。

### 3.4 v11 几何硬投影不变

每条层纹继续使用 v11 的双 logistic 有限宽剖面：横向过渡 `0.38 px`，端点过渡 `0.55 px`。三轮闭环使用与审计相同的FWHM算子校准宽度。中心线、端点、长度、宽度和夹层全部来自盲去噪测量约束；细节引导不能移动这些量。

### 3.5 新增结构细节审计

除原有层纹/夹层长宽审计外，v13 新增：

- 前景低频相关性（Gaussian `sigma=4`）；
- 中频相关性（`G_0.8-G_4`）；
- 梯度幅值相关性；
- 100条层纹逐条轴向对比度相关性。

逐层明细写入 `structure_detail_consistency.csv`，聚合结果写入 `hard_shape_projection_metrics.json`。

## 4. 本图实验结果

| 指标 | v13结果 |
|---|---:|
| 约束匹配层纹 | 49左 + 51右 = 100 |
| 约束匹配夹层 | 98 |
| 层纹/夹层双证据通过 | 100/100；98/98 |
| 引导坐标端点误差P95 | 0.0425 px |
| 引导坐标长度误差P95 | 0.0555 px |
| 层纹FWHM相对误差中位数 / P95 | 0.0124% / 0.1521% |
| 夹层宽度相对误差P95 | 0.0830% |
| 目标 / 输出层纹中位宽度 | 4.251057 / 4.251422 px |
| 原图 / 引导 / 输出边缘清晰度 | 0.3061 / 0.2976 / 0.3631 |
| 软生成 / 最终低频结构相关性 | 0.9495 / 0.9141 |
| 软生成 / 最终中频结构相关性 | 0.7060 / 0.6597 |
| 软生成 / 最终轴向细节相关性中位数 | 0.3212 / **0.8884** |
| 最终轴向细节相关性P10 | 0.8120 |

硬投影为了消除生成器错误周期并重建精确FWHM，使全局低频相关性相对软生成下降0.0354，但仍高于0.90且处于0.05容许范围内；更关键的逐层轴向细节相关性提高0.5672。所有几何、清晰度和细节门限均通过。

## 5. 代码和运行

```bash
python app/generative_postprocess.py \
  --generated results_generative_shape_v13/01_generated/GENERATIVE_v13_blind_guide_detail_conditioned_raw.png \
  --outdir results_generative_shape_v13/02_postprocessed

python app/generative_shape_project.py \
  --profile v13 \
  --source input/source_16bit.tif \
  --guide results_sota/01_sota_blind/MEASUREMENT_sota_geometry_blind_16bit.tif \
  --generated results_generative_shape_v13/02_postprocessed/GENERATIVE_visual_only_postprocessed.png \
  --outdir results_generative_shape_v13/03_detail_consistent_projection \
  --detail-guide-weight 0.55
```

`--detail-guide-weight` 只控制结构细节融合，不能改变几何坐标。正式试验比较了 `0.35/0.45/0.55`，最终选择0.55，因为它在全部几何门限通过的条件下获得最高结构相关性。

## 6. 使用边界

最终图仍含生成内容，只能用于视觉增强、算法研究和辅助观察，不能作为计量认证图或无噪声真值。正式厚度、长度与缺陷判定必须使用非生成式16位测量链、像素尺寸标定和系统PSF/MTF验证。
