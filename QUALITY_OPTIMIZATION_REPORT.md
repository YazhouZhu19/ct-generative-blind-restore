# 图像质量优化说明（不包含层纹长度测量）

## 结论

本轮继续使用上一版单图自监督盲去噪模型，不使用生成式模型重绘层纹，也不以层纹长度或端点为优化目标。最终输出为完整的 2200 × 1600、16 位灰度 TIFF：

`results_quality/QUALITY_balanced_16bit.tif`

质量优化由两步组成：先用 Noise2Self/APR-RD 思路的盲点预测降低随机噪声，再在盲去噪结果上做方向受限、幅度受限的边缘与局部结构增强。

## 方法

1. **单图自监督盲去噪**：训练时把随机中心像素替换为相邻像素，仅在被遮挡位置计算损失，降低网络直接复制噪声的风险。
2. **多掩膜集成**：以多组随机掩膜预测同一像素，输出均值和不确定性；最终盲去噪结果通过数据一致性与边缘门控回写。
3. **方向性边缘增强**：片层近似竖直，因此仅沿水平方向分离细节，增强垂直层纹两侧的横向梯度，避免沿片层长度方向制造尖端。
4. **结构增强**：使用小尺度与大尺度高斯差分提升片层和背景的分离度，不对完整 16 位图做激进直方图重映射。
5. **幅度限制**：每个像素的变化量由盲去噪图的 MAD 噪声估计限制，抑制光晕、双边和过冲。
6. **自动参数选择**：搜索边缘增益与结构增益组合，以边缘锐度、局部对比度和平坦区高频起伏构成目标函数。

## 结构保护条件

候选结果必须同时满足：

- 左右片层检测数量相对上一版变化不超过 1；
- 层中心最大位移不超过 1 像素；
- 中位 FWHM 相对变化不超过 5%；
- 平坦区高频起伏相对上一版不超过 3%；
- 与上一版盲去噪结果的 ROI SSIM 不低于 0.985。

这些条件用于避免清晰度增强改变层纹拓扑，但不等同于有标准真值下的精度认证。

## 本图结果

自动搜索选择：`edge_amount = 0.32`，`structure_gain = 0.06`。

- 相对原始图，平坦区高频起伏降低约 23.0%；
- 相对原始图，数字边缘锐度约提升 5.9%；
- 相对原始图，局部对比度约提升 3.0%；
- 相对上一版盲去噪图，数字边缘锐度约提升 8.1%；
- 相对上一版盲去噪图，局部对比度约提升 3.5%；
- 相对上一版盲去噪图，平坦区高频起伏增加约 2.1%，仍在 3% 守卫线内；
- 最大层中心位移 1 像素，中位 FWHM 最大相对变化约 2.62%；
- ROI SSIM 为 0.99857，全部守卫条件通过。

这里的“数字边缘锐度提升”只是图像域梯度指标，不代表扫描设备的物理分辨率提高。

## 输出选择

- `QUALITY_balanced_16bit.tif`：推荐主结果；完整 16 位图，无生成式像素。
- `QUALITY_balanced_preview.png`：推荐主结果的完整 8 位预览。
- `QUALITY_display_only.png`：额外局部对比映射，仅用于屏幕观看。
- `QUALITY_comparison.png`：四宫格局部放大对比。
- `quality_metrics.json`：全部候选参数、指标和守卫结果。

## 复现

```bash
docker compose run --rm ct-restore-cpu
docker compose run --rm ct-quality
```

也可在已有 Python 环境中直接运行：

```bash
python app/quality_optimize.py \
  --source input/source_16bit.tif \
  --baseline results/MEASUREMENT_blind_denoised_16bit.tif \
  --outdir results_quality
```

脚本固定随机之外的搜索流程；本阶段本身是确定性的。若换图，应先确认默认 ROI 是否覆盖片层目标，必要时通过 `--target-roi`、`--left-roi` 和 `--right-roi` 修改。
