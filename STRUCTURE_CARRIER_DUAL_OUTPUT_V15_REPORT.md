# v15 结构载体双输出技术报告

## 1. 问题与结论

v11/v13 能把生成图的层纹中心线、端点、宽度和夹层几何投影到盲去噪约束上，但解析重绘仍会改变每条层纹真实的局部起伏、缺口、轴向灰度变化和夹层纹理。对于长度与厚度测量，几何统计量接近并不等于像素级结构一致。

v15 不再要求一张图同时承担“生成式美化”和“定量测量”两种相互冲突的任务，而是输出两个语义严格分离的文件：

```text
原尺寸生成图 -> 原后处理 -> v13细节投影 -> VISUAL_ONLY视觉图
原图 -> 盲去噪/几何锁定 -----------------> MEASUREMENT结构载体图
```

视觉图保留生成式增强能力；测量图的生成像素权重为零。两者均为2200×1600，但只有测量分支可用于后续检测算法的输入候选，最终物理量仍需像素标定和系统PSF/MTF验证。

## 2. 方法创新

### 2.1 从“参数约束”升级为“观测数据一致性”

旧方法只约束层纹的中心、长宽和端点，生成器仍可在约束之间改变局部结构。v15 把同坐标盲去噪图定义为结构载体：测量输出的每个像素均来自该载体，生成图不再参与测量输出的灰度或纹理。

### 2.2 禁止隐式结构变换

测量分支默认执行以下硬约束：

- 不缩放、不重采样、不配准、不形变；
- 不进行解析层纹重绘；
- 不把生成图的低频、纹理或背景混入测量图；
- 不对结构载体执行显示范围映射，因为饱和裁剪也可能移动半高宽交点；
- 保留可选但默认关闭的边缘自适应残差收缩和横向反锐化参数，避免未经验证的后处理静默改变测量结果。

### 2.3 双输出而非混合ROI

实验曾尝试仅在层纹ROI内使用盲去噪图、ROI外使用生成图。尽管ROI内部结构相关性接近1，该方案会在ROI边界形成可见拼接缝，且难以向下游算法清晰声明哪些像素是观测、哪些像素是合成。因此最终实现采用全幅结构载体测量图，并把生成结果单独输出。

### 2.4 多尺度结构审计

除100条层纹和98个夹层的端点、长度、FWHM与间距审计外，v15 还比较：

- 高斯 `sigma=4` 的低频结构相关性；
- `G_0.8-G_4` 中频结构相关性；
- 平滑梯度幅值相关性；
- 每条层纹相对左右夹层的轴向对比度曲线相关性。

审计明细写入 `structure_detail_consistency.csv`、`lamella_dual_evidence_comparison.csv`、`interlayer_dual_evidence_comparison.csv` 和 `hard_shape_projection_metrics.json`。

## 3. 本图结果

| 指标 | v15测量分支 | v15视觉分支 |
|---|---:|---:|
| 图像尺寸 | 2200×1600 | 2200×1600 |
| 生成像素权重 | 0 | 非零，仅视觉 |
| 层纹检出 | 49左 + 51右 = 100 | 100 |
| 夹层数量 | 98 | 98 |
| 层纹宽度误差中位数 / P95 | 0% / 0% | 0.0169% / 0.2157% |
| 层纹端点误差P95 | 0 px | 0.1603 px |
| 层纹长度误差P95 | 0.0599 px | 0.1954 px |
| 夹层宽度误差P95 | 0% | 0.1404% |
| 夹层长度误差P95 | 0.0799 px | 0.1655 px |
| 低频结构相关性 | 1.0000 | 0.9414 |
| 中频结构相关性 | 1.0000 | 0.6474 |
| 梯度相关性 | 1.0000 | 0.4842 |
| 单层轴向细节相关性中位数 / P10 | 1.0000 / 1.0000 | 0.8913 / 0.8221 |
| 边缘清晰度相对原图 | -2.78% | +17.58% |

测量分支的边缘清晰度略低于原始图，是盲去噪抑制噪声的结果；它与测量引导图完全一致，并通过全部几何、原图非退化、夹层和清晰度门槛。视觉分支更清晰，但不能用于定量测量。

## 4. 代码与复现

完整入口：

```bash
python app/run_v15_pipeline.py \
  --source input/source_16bit.tif \
  --guide input/measurement_guide_16bit.tif \
  --generated input/generative_candidate_visual_only.png \
  --outdir results_generative_shape_v15_dual_output
```

Docker入口：

```bash
docker compose run --rm ct-v15-dual
```

核心实现位于 `app/generative_shape_project.py` 的 `structure_carrier_projection()`；单命令编排位于 `app/run_v15_pipeline.py`。本次在 `ct-generative-blind-restore:cpu` 容器中完成全流程验证；加入v16守卫测试后，当前32项单元测试全部通过。

## 5. 输出使用边界

- `FINAL_VISUAL_ONLY_*.tif`：用于人工观察、展示和模型研究，禁止直接测量。
- `FINAL_MEASUREMENT_structure_preserved_*.tif`：可作为检测/测量算法的图像输入候选，但数值结果应与导出的层纹/夹层CSV交叉验证。
- 从像素换算为毫米或微米前，必须提供可靠像素尺寸，并完成扫描系统PSF/MTF与标准样件验证。
- 当前结果是单幅内部验证，不构成计量认证或跨设备泛化证明。
