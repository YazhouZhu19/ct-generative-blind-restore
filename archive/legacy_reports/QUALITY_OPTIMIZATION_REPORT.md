# 图像质量、边界保真与残留雾净化说明 v4

## 结论

v4 完整保留原单图盲去噪和质量后处理算法。原质量搜索仍选择 `edge_amount=0.32`、`structure_gain=0.06`，随后才在最终增强图上执行一次局部亚像素几何校正。最后追加受锐度、对比度、SSIM和逐层几何守卫约束的弱结构感知净化。原图只提供端点坐标，不回写原始像素。

推荐结果：`results_quality/QUALITY_boundary_preserved_16bit.tif`。

详细原理见 [`BOUNDARY_PRESERVATION_REPORT.md`](BOUNDARY_PRESERVATION_REPORT.md)。

## 原增强算法保持不变

以下步骤及其参数均先完整执行：

1. Noise2Self/APR 启发的单图盲点训练；
2. 多掩膜集成与不确定性估计；
3. 数据一致性和梯度门控；
4. 沿层纹方向的连续性处理；
5. 原质量网格搜索；
6. 横向边缘增强、DoG 结构增强、MAD 幅度限制和 ROI 羽化。

未校正的原增强结果单独保存为 `QUALITY_original_enhancement_16bit.tif`。

## 后续几何校正

沿每条层纹中心路径测量原图与原增强图的上下端点差，然后在增强图上建立半径1 px的局部位移场。常规位移上限为0.35 px；超过3 px的竞争峰关联异常允许一次不超过1 px的局部重关联。三次插值仅采样增强像素，不读取原图灰度，因此不会降低原有去噪能力。

## 结构外弱净化

几何校正结果先保存为 `QUALITY_geometry_only_16bit.tif`。随后在低梯度区域以0.12强度抑制细颗粒，以0.015强度抑制中尺度雾状残留；强边缘及端点坐标保护区不处理，改变量按噪声MAD限幅并在ROI边界羽化。程序同时试验多个弱强度档，只选择通过全部守卫且粗糙度降低最大的候选。

## 守卫条件

- 原质量阶段继续使用原层数、中心、FWHM、噪声和 SSIM 守卫；
- 最终中位 FWHM 相对原图变化不超过 4%；
- 可靠层端点偏移绝对 95% 分位不超过 0.35 px，最大不超过 0.75 px；
- 可靠层长度差绝对 95% 分位不超过 0.50 px，最大不超过 1.00 px；
- 原始端点不确定度超过 3 px 或 SNR 不足的层标记为 `review`。
- 净化后的边缘锐度保留率不低于99%，局部对比度保留率不低于99.5%；
- 净化结果相对几何校正图的SSIM不低于0.9999。

## 本图结果

- 相对原图高频噪声降低：26.28%；
- 相对原图数字边缘锐度提升：4.89%；
- 相对原图局部对比度提升：2.72%；
- 几何校正前后 SSIM：0.999996；
- 净化相对几何阶段的结构外粗糙度降低：4.54%；
- 净化阶段边缘锐度/局部对比度保留：99.09%/99.72%；
- 净化相对几何阶段 SSIM：0.999951；
- 最终端点偏移绝对 95% 分位：0.261 px；
- 最终可靠层最大端点偏移：0.695 px；
- 最终长度差绝对 95% 分位：0.433 px；
- 最终可靠层最大长度差：0.704 px；
- 相对原图最大中位 FWHM 变化：0.188%；
- 外观、宽度、端点和长度守卫全部通过。

## 输出

- `QUALITY_original_enhancement_16bit.tif`：原增强与后处理结果，不含几何校正。
- `QUALITY_geometry_only_16bit.tif`：几何校正后、弱净化前的审计图。
- `QUALITY_boundary_preserved_16bit.tif`：推荐v4结果。
- `QUALITY_balanced_16bit.tif`：推荐结果兼容名称。
- `QUALITY_comparison.png`：原图、盲去噪、原增强、v4结果对比。
- `QUALITY_fog_cleanup_closeup.png`：净化前后近照和放大差值。
- `QUALITY_boundary_overlay.png`：端点重合检查。
- `boundary_geometry.csv`、`quality_metrics.json`：逐层数值与审计记录。

数字边缘锐度不等于扫描仪物理分辨率。绝对物理长度仍需像素尺寸和系统标定。
