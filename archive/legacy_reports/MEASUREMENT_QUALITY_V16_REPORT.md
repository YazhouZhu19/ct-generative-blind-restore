# v16 结构测量图质量优化报告

## 目标

v15 测量图与盲去噪结构载体逐像素一致，结构安全性最高，但中央实体块仍存在可见颗粒。v16 的目标是在不引入生成像素、不做空间变换且不改变层纹/夹层测量的前提下，进一步降低该区域的高频噪声。

## 方法

`app/measurement_quality_optimize.py` 以 v15 图为不可覆盖基线，构造零相位、幅度受限的候选：

- 层纹内部候选使用纵向残差收缩，并保护横向梯度与上下端点；
- 中央实体块候选使用羽化非局部均值去噪；
- 单像素变化不超过盲噪声 MAD 导出的上限；
- 不读取或写入任何生成图像像素；
- 不缩放、不配准、不形变。

每个候选重新审计100条层纹和98个夹层，并要求同时通过端点、长度、FWHM、夹层宽度/长度、原图非退化、边缘清晰度、多尺度结构相关性和SSIM门槛。

## 参数选择

初始较强候选虽然使层纹内部轴向噪声下降5.87%，但独立逐层审计发现层纹宽度P95变化达到4.80%，因此被拒绝。更弱的层纹强度仍使少数临界细纹的双证据状态退化。

最终选择：

```text
lamella_strength = 0.00
central_strength = 0.92
```

即层纹和夹层像素保持v15不变，只对中央实体块进行羽化非局部均值去噪。

## 当前图像结果

| 指标 | v16结果 |
|---|---:|
| 输出尺寸/类型 | 2200×1600 / uint16 |
| 生成像素权重 | 0 |
| 中央区域高频噪声下降 | 9.77% |
| 层纹区域附加处理强度 | 0 |
| 层纹宽度误差中位数/P95 | 0% / 0% |
| 层纹端点误差P95 | 0 px |
| 层纹长度误差P95 | 0.0599 px |
| 夹层宽度误差P95 | 0% |
| 夹层长度误差P95 | 0.0799 px |
| 低频/中频结构相关性 | 1.0000 / 0.9999 |
| 梯度相关性 | 0.99985 |
| 单层轴向细节相关性中位数/P10 | 1.0000 / 1.0000 |
| SSIM（相对v15） | 0.99963 |

全部严格守卫条件通过。

## 运行

```bash
python app/measurement_quality_optimize.py \
  --source input/source_16bit.tif \
  --input results_generative_shape_v15_dual_output/FINAL_MEASUREMENT_structure_preserved_2200x1600_16bit.tif \
  --outdir results_generative_shape_v16_measurement_quality
```

或：

```bash
docker compose run --rm ct-v16-measurement-quality
```

输出 `MEASUREMENT_v16_quality_enhanced_16bit.tif`。v15 原始结构载体仍应保留，用作回溯基线和交叉验证。

## 限制

v16 有意不继续平滑层纹主体，因此层纹区域的视觉噪声改善有限。这是严格测量约束下的安全选择，而不是算法失效。若需要同时显著降低层纹噪声并证明亚像素尺寸无偏，需要同设备重复扫描、配对高剂量真值或标准样件进行训练与验证。
