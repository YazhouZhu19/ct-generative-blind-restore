# v10 生成式形状约束报告

## 方法

v10 将原图、结构条件和外观参考分开：

1. 原始16位图负责测量几何。
2. `app/generative_shape_constraint.py` 检测每条层纹的弯曲中心路径和亚像素上下端点，并沿路径多截面测量横向FWHM。
3. 相邻层纹的夹层宽度定义为中心距减去两侧层纹半宽之和；夹层长度定义为相邻层纹共同存在的纵向区间。
4. 生成条件图用白色表示层纹包络、绿色表示低频拟合中心线、红色表示端点、蓝色表示夹层中心线。它与原图及未配准外观参考图共同送入内置生成模型。
5. 软条件生成仍可能合并细层纹，因此 `app/generative_shape_project.py` 再把生成图的低频亮度、背景和纹理投影到解析层纹包络。横向使用以实测FWHM为宽度的Gaussian剖面，纵向半高点固定在实测亚像素端点。

这里的中心线只去除逐行跟踪产生的扫描噪声抖动，使用纵向 `sigma=45 px` 的低频拟合；各条层纹长宽和夹层长宽不从生成图重新估计。

## 原图测量

| 指标 | 数值 |
|---|---:|
| 左侧层纹 | 49 |
| 右侧层纹 | 51 |
| 夹层 | 98 |
| 层纹中位宽度 | 4.245 px |
| 层纹宽度P05–P95 | 3.644–5.084 px |
| 层纹中位长度 | 525.504 px |
| 夹层中位宽度 | 6.838 px |
| 夹层中位共同长度 | 486.082 px |

逐条数值位于 `lamella_geometry.csv` 和 `interlayer_geometry.csv`。

## 最终约束审计

硬投影解析生成100条层纹。常规峰值检测得到左侧49条、右侧50条；右侧末端两条中心相距约6.44 px，检测器把它们合并，但解析投影和逐条端点审计仍包含全部51条右侧层纹。

| 指标 | 结果 |
|---|---:|
| 约束坐标端点偏移P95 | 0.083 px |
| 约束坐标长度变化P95 | 0.111 px |
| 宽度相对误差中位数 | 4.54% |
| 宽度相对误差P95 | 12.33% |
| 输出中位宽度 | 4.497 px |

原始逐行噪声轨迹还作为独立诊断保留；它与低频物理中心线不同，不能作为最终生成条件的验收坐标。

## 输出性质

这一结果比纯生成更能保持层数、长宽和夹层，但其灰度纹理仍由生成模型合成，不是计量真值。它适合观察、展示和算法原型比较；厚度、长度或缺陷验收仍应使用非生成式测量链和标定数据。
