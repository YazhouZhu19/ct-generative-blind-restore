# v7 几何安全残差去噪报告

## 结论

v7 在 v6 边界清洁之后增加区域分离的残差去噪，同时保留生成先验盲模型、原增强/后处理和 v6 边界清洁的全部步骤。推荐的完整 2200 × 1600、16 位灰度结果为：

```text
results_sota/04_residual_denoise/QUALITY_v7_residual_denoised_16bit.tif
```

正式图像中，左右测量区的平坦区域高频噪声相对原图降低 **38.38%**，高于 v5 的28.65%和 v6 的35.37%。中央高亮内部噪声相对原图降低 **19.91%**。最终数字边缘锐度仍比原图高 **5.39%**，局部对比度高 **2.02%**。

## 混合去噪方法

### 层纹区域

层纹内部使用 `σ=(2.50,0.18)` 的零相位轴向滤波。只收缩幅值符合噪声统计的残差，并使用横向梯度门保护厚度边缘：

\[
r=I-G_{(2.50,0.18)}I,
\quad w_n=\exp[-(|r|/(2.6\sigma_n))^4]
\]

\[
I'=I-\alpha M_{inside}M_{edge}M_{endpoint}w_n r
\]

这种方式主要沿片层长度方向处理颗粒波动，不跨越左右厚度边缘。

### 中央高亮

中央高亮不属于层纹端点包络，因此使用独立的快速非局部均值：`patch=5`、`distance=5`、`h=0.85σ`。结果通过18像素软掩膜混合，中央块边界不参与平滑。

### 几何保护

- 原始图只提供100条层纹的端点坐标，不回写原始像素。
- 上下端点核心邻域不可修改。
- v7 单像素变化限制为 `0.65×MAD`。
- 参数必须通过层数、FWHM、端点、长度、边缘锐度、局部对比度、中央均值及SSIM守卫。
- 独立长度审计使用固定的 v6 几何向导，防止去噪前后重新检测路径造成虚假差异。

## 正式结果

| 指标 | v7结果 |
|---|---:|
| v7额外层纹轴向噪声降低 | 6.97% |
| v7额外中央噪声降低 | 8.41% |
| v5→v7层纹轴向噪声累计降低 | 16.38% |
| 相对原图平坦区高频噪声降低 | 38.38% |
| 相对原图中央区噪声降低 | 19.91% |
| 相对原图边缘锐度增益 | 5.39% |
| 相对原图局部对比度增益 | 2.02% |
| 横向边缘锐度相对v6保持率 | 99.78% |
| v7相对v6 SSIM | 0.998548 |
| 最大中位FWHM变化 | 1.23% |

固定几何向导的独立审计检测到100条层纹，98条自动通过，2条因原始端点参考本身不确定而复核。可靠层纹端点偏移P95为0.248像素，长度变化P95为0.402像素，最大长度变化为0.499像素，全部几何守卫通过。

## 复现

完整五阶段容器流水线：

```bash
docker compose run --rm ct-sota
```

单独运行v7：

```bash
python app/residual_denoise.py \
  --source input/source_16bit.tif \
  --input results_sota/03_boundary_cleanup/QUALITY_boundary_clean_16bit.tif \
  --outdir results_sota/04_residual_denoise
```

测量前检查 `residual_denoise_metrics.json`、`QUALITY_v7_boundary_overlay.png` 和 `05_length_audit/length_qa.json`。

## 限制

结果仍属于单图内部验证。绝对物理长度需要像素尺寸标定、标准件、PSF/MTF和重复扫描验证。算法不会用生成图或参考图像素替换测量图，但无配对真值时不能给出绝对去噪误差。
